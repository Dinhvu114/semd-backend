jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:900000}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:604800000}

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/semd_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123456}
    driver-class-name: org.postgresql.Driver

  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: ${PORT:8080}

# ==========================================
# MinIO
# ==========================================
minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:admin}
  secret-key: ${MINIO_SECRET_KEY:admin123456}
  bucket-name: ${MINIO_BUCKET_NAME:ems-cad-audio}

# ==========================================
# App Config
# ==========================================
app:
  callback-key: ${SPRING_BOOT_CALLBACK_KEY:ems-cad-callback-secret-key-789012}

  osrm:
    base-url: ${OSRM_BASE_URL:http://localhost:5000}
    profile: driving
    connect-timeout-ms: ${OSRM_CONNECT_TIMEOUT_MS:2000}
    read-timeout-ms: ${OSRM_READ_TIMEOUT_MS:5000}
    max-retries: ${OSRM_MAX_RETRIES:2}

  simulation:
    scheduler-pool-size: 4
    default-tick-interval-ms: 1000
    location-log-interval-ms: 5000
    stale-session-threshold-seconds: 30

  otp:
    delivery-mode: ${OTP_DELIVERY_MODE:mock}
    ttl-seconds: ${OTP_TTL_SECONDS:300}
    resend-cooldown-seconds: ${OTP_RESEND_COOLDOWN_SECONDS:60}
    registration-token-ttl-seconds: ${OTP_REGISTRATION_TOKEN_TTL_SECONDS:600}
    max-verification-attempts: ${OTP_MAX_VERIFICATION_ATTEMPTS:5}
    default-country-code: ${OTP_DEFAULT_COUNTRY_CODE:+84}
    hash-secret: ${OTP_HASH_SECRET:${jwt.secret}}

  sms:
    endpoint: ${SMS_ENDPOINT:}
    api-token: ${SMS_API_TOKEN:}
    sender: ${SMS_SENDER:SEMD}
    message-template: ${SMS_MESSAGE_TEMPLATE:Ma xac thuc SEMD cua ban la %s. Ma co hieu luc trong 5 phut.}